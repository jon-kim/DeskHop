<Window x:Class="ScreenManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Screen Manager" Height="360" Width="600" MinHeight="320" MinWidth="520"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Open app windows" FontWeight="SemiBold" />
        <ComboBox x:Name="WindowSelector"
                  Grid.Row="1"
                  Margin="0,8,0,16"
                  DisplayMemberPath="DisplayName"
                  MinHeight="28"
                  SelectionChanged="Selector_SelectionChanged" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Move to monitor" VerticalAlignment="Center" FontWeight="SemiBold" />
            <ComboBox x:Name="MonitorSelector"
                      Margin="12,0,0,0"
                      Width="260"
                      DisplayMemberPath="DisplayName"
                      MinHeight="28"
                      SelectionChanged="Selector_SelectionChanged" />
            <Button Content="Refresh"
                    Margin="12,0,0,0"
                    Width="90"
                    Click="RefreshButton_Click" />
            <Button x:Name="MoveButton"
                    Content="Move"
                    Margin="8,0,0,0"
                    Width="90"
                    Click="MoveButton_Click" />
        </StackPanel>

        <TextBlock x:Name="StatusText"
                   Grid.Row="3"
                   Margin="0,16,0,0"
                   Foreground="DimGray"
                   TextWrapping="Wrap" />
    </Grid>
</Window>
